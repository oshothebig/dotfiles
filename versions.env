# Single source of truth for pinned tool versions.
# Consumed by both the Cloud Agent bootstrap (.cursor/install.sh) and the
# Format check workflow (.github/workflows/format.yaml).
# Format: KEY=value (no quotes) so it can be `source`d by bash and appended to
# $GITHUB_ENV in CI.
JUST_VERSION=1.50.0
YAMLFMT_VERSION=0.21.0
